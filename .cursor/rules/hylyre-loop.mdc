---
description: Hylyre 原子循环模式 — 外部 Agent 用 dump-ui/screenshot + planned JSON，无需运行态 VLM
globs: "**/*.md"
alwaysApply: false
---

# Hylyre Agent 循环模式

当用户要用 **自然语言描述用例** 并在 **真机** 上跑通，且 **不想配 `HYLYRE_VLM_*`** 时：

1. 优先 **[docs/agent-loop.md](docs/agent-loop.md)**：CLI 与 MCP **能力对等**（除 MCP-only `open_session` 连接复用）。
2. **事实源**：非多模态 → `hylyre dump-ui` / `hylyre_dump_ui`；多模态 → `hylyre screenshot` / `hylyre_screenshot`。
3. **执行**：生成 **[做法 A](docs/agent-plan-a.md)** 允许的 JSON（根键 `action` / `touch` / `input` / **`swipe`** / **`scroll`**），或使用 **`hylyre run action|tap|input|swipe|scroll`**（及 MCP **等价工具**，包括 **`hylyre_run_swipe`** / **`hylyre_run_scroll`**）。
4. **报告**：长会话用 `hylyre report begin|record|finalize` 或 MCP `hylyre_report_*`；批量回归仍可用 `hylyre run --plan …` / `hylyre_run_plan`。
5. **自然语言未写手势时**：必读 **`docs/agent-loop.md`** 中的 **「自然语言未约定手势时」** 一节：先 **`dump-ui`** 再决定是否滑动；竖向列表在 **`Scroll` 内**露出下方条目优先 **`UP`**；半屏列表必须 **`area` / `at`**；滑动后对比相邻 **`dump-ui`**，树不变则调整参数而非直接下结论。
6. **批处理优先**：仅当用户明确要求「整份 test-plan 一次性跑完」时才主推做法 A + `hylyre run --plan`。

不要默认假设你能「看见」用户 App；必须先 dump-ui 或截图（或用户给出稳定 `by_id`/`by_text`）。
