---
description: Hylyre 默认行为 — 如何让 AI 稳定调用本仓库 CLI 或 MCP（不需用户每轮复述）
alwaysApply: true
---

# Hylyre 仓库内 AI 助手

## 目标

用户希望「方便地用 Hylyre 做真机/离线测试」。**不要**在每一轮对话开头要求用户重复安装步骤；优先读本仓库 **`AGENTS.md`**，并遵守下面优先级。

## 调用方式（按优先级）

1. **若当前会话已连接 Hylyre MCP**（工具名形如 `hylyre_run_plan`、`hylyre_doctor`）：优先用 MCP，路径参数用**绝对路径**。
2. **否则**：在**仓库根目录**执行 **`python -m hylyre …`**（与 CLI `--help` 一致）。**能力与 MCP 对等**（MCP 为薄壳）。**CLI** 长时间原子循环可用 **`hylyre session start`** + **`--session`**；**MCP** 进程内复用见 **`hylyre_open_session`**（说明见 **`docs/agent-loop.md`**）。

## 批量步骤（优先于逐个单步）

已知 **2 个及以上连续** planned JSON 步骤（如已从 **`hylyre_app_find`** 拿到的导航、`app page` 里确认过的 **`by_id`** 序列，或上一轮循环已验证的步骤）：

- **优先**：MCP **`hylyre_run_steps`**（传入 `steps` 数组）；或 CLI **`python -m hylyre run --steps-file *.json`**，并 **`--session`** / MCP 已 **`hylyre_open_session`**。
- **不要**连续多次只调 **`hylyre_run_tap`** / **`hylyre_run_swipe`**（除非每步都必须读 **`dump_ui`/`screenshot` 后再决定下一步）。

仅在步骤不确定或需逐步感知时再走单步 **`run_*`** / 原子循环。

## 自然语言测试意图 → 可执行输入

当用户要「不配运行态 VLM」时：

- **回归计划 / 已知 selector**：**做法 A** — 编写或修改 **`*test-plan*.md`**（规约见 **`docs/agent-plan-a.md`**、`hylyre-plan-a.mdc`）。
- **先感知界面再逐步执行**：**原子循环** — **`docs/agent-loop.md`**（含 **`_hylyre_hints`**、**`collect-list`**、**Knowledge-first** 下的 **`app page load` / `app find`**（见 **`docs/app-knowledge.md`**）、**「自然语言未约定手势时」**：未写滑动时先 dump、再在 `Scroll` 内用 **`UP` 等** + **`area`/`at`** 并校验相邻 dump）。CLI 与 MCP 镜像。

## 常用参数意图

- **快速验证链路**：`--use-fakes` + fixture `tests/e2e/fixtures/mock-test-plan.md`。
- **真机**：安装 **`hylyre[device]`**，`--device-sn`、`--bundle`；JSON 步骤可无 **`HYLYRE_VLM_*`**；若「预期结果」列不要 VLM 断言，用 **`--skip-assert-expected`**。
- **自然语言步骤或 `hylyre_ai_*`**：需要 **`HYLYRE_VLM_*`**（及设备能力）。

## MCP 一次性配置

若用户尚未启用 MCP：指引阅读 **`docs/cursor-mcp-setup.md`**，配置一次即可，无需写进每条用户消息。
