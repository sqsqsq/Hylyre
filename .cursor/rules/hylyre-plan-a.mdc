---
description: Hylyre 做法 A — 将自然语言测试意图写进 test-plan.md 的 JSON 步骤（无运行态 VLM）
globs: "**/*test-plan*.md"
alwaysApply: false
---

# Hylyre 做法 A（JSON 测试步骤）

当用户要**真机跑 Hylyre**且**不想依赖运行态 VLM**理解步骤时：把「测试步骤」列写成**单行 JSON**，不要用自然语言段落（自然语言会触发 `ai_action`，必须有 `HYLYRE_VLM_*`）。

## 表格约束

- 文档中须有标题 **`## 测试用例清单`** 或 **`### 测试用例清单`**。
- 表头 7 列顺序：`用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 关联 AC`。
- 同一单元格内多步：用 **`;` 或 `；`** 分隔多条 JSON，不要用 `<br/>`。
- 全表避免出现会破坏表格的 **`|`**（尤其步骤列 JSON 内的字符串）。
- **不要**用 markdown 反引号包裹步骤 JSON；直接写裸 JSON。
- **不要**用 CLI 名（`tap`/`swipe`）作 JSON 根键。

## 步骤 JSON（根键唯一；推荐 direct，action 可选）

1. **touch**（推荐，与 `run_planned_tap` 一致）
   - `{"touch":{"by_text":"充值"}}` 或 `{"touch":{"x":10,"y":20}}`
2. **input**（推荐）
   - `{"input":{"text":"hello","by_id":"field","by_text":null}}`
3. **action**（可选信封，与 `run_planned_action` 一致）
   - `{"action":{"type":"touch","by_text":"登录"}}`
   - `{"action":{"type":"swipe","direction":"UP","distance":55,"area":{"by_type":"Scroll"}}}`
4. **swipe** / **scroll**（与原子 CLI `run swipe` / `run scroll`、MCP `hylyre_run_swipe` / `hylyre_run_scroll` 一致；竖向列表、半屏模态见 **`docs/agent-loop.md`**）
   - `{"swipe":{"direction":"UP","distance":65,"area":{"by_type":"Scroll"}}}`
   - `{"scroll":{"direction":"down","steps":8,"at":{"by_type":"Scroll"}}}`
5. **Tier A 导航 / 等待 / Toast**（详见 **`docs/agent-plan-a.md`** §2.1）
   - `{"back":{}}` — Nav 子页返回（**不是** `swipe RIGHT` 冒充返回）
   - `{"home":{}}`、`{"wait":{"seconds":1.5}}`
   - `{"wait_for":{"by_text":"钱包","timeout":10}}`、`{"assert_toast":{"text":"…","timeout":3}}`
   - `{"stop_app":{"bundle":"com.example.app"}}`、`{"start_app":{"bundle":"com.example.app"}}`

**禁止**使用 `{"action":{"name":"back"}}`（无效；应写 `{"back":{}}` 或 `{"action":{"type":"back"}}`）。

翻译用户意图时：优先 **by_id**（稳定）；只有没有 id 时才用 **by_text** 或坐标。需要固定滚动语义时在步骤 JSON 中写明 **`swipe`/`scroll`**（勿依赖对话 Agent 猜测）；参见 **`docs/agent-loop.md`**「自然语言未约定手势时」对照原子循环纪律。

## 预期结果

不配 VLM 时，「预期结果」列**不会**被执行器自动断言（仅供人读）。需要自动断言则配置 `HYLYRE_VLM_*` 或接受仅执行 JSON 步骤。

## 详情

见仓库 `docs/agent-plan-a.md` 与示例 `tests/e2e/fixtures/json-steps-test-plan.md`。
