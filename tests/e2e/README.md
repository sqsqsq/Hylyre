# e2e/ — run locally with `pytest -m e2e` when hardware available.

Fixture **`fixtures/json-steps-test-plan.md`**：做法 A 示例（步骤列为 JSON，无运行态 VLM）。说明见 [`docs/agent-plan-a.md`](../../docs/agent-plan-a.md)。
